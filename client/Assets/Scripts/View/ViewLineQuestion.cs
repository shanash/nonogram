using UnityEngine;
using System.Collections.Generic;

public class ViewLineNumber : MonoBehaviour
{
    public enum Direction
    {
        None = 0,
        Vertical,
        Horizon,
    }
    public void Init(Direction dir, RectTransform parent, List<int> data, float tileSize)
    {
        
    }
}
